<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youngtube.demo.mapper.VideoMapper">


    <select id="find8ByCategory" resultType="video">
        select * from video limit 8
    </select>

    <select id="findWithRecommend" resultType="video">
        select * from video limit 6
    </select>

    <select id="find7ByCategoryAndRank" resultType="video">
        select videoId,videoName,videoSrc from video order by videoViewCount desc limit 7
    </select>

    <select id="findOneById" resultType="video">
        select * from video where videoId=#{videoId}
    </select>

</mapper>